{
    "name": "masked-rider-tile-game",
    "version": "1.0.0",
    "description": "Masked Rider Tile Puzzle Game",
    "main": "index.js",
    "scripts": {
        "start": "npx serve .",
        "build": "rm -rf dist && mkdir dist && cp *.html *.js *.css dist/ && cp -r assets dist/ && touch dist/.nojekyll",
        "deploy": "npm run build && gh-pages -d dist"
    },
    "keywords": [
        "game",
        "masked-rider",
        "puzzle"
    ],
    "author": "",
    "license": "ISC",
    "devDependencies": {
        "gh-pages": "^6.3.0"
    }
}